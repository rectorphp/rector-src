<?php

function dummy_wrap($maxWidth, $text)
{
    if (get_font_set()) {
    } else {
        $nb = strlen($s);

        if ($nb > 0 && $s[$nb - 1] == "\n") {
            $nb--;
        }
    }

    $nl  = 1;

    return $nl;
}

?>
